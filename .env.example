# ============================================================
# OpenHands + GitLab 連携設定
# cp .env.example .env して値を埋めてください
# ============================================================

# ─── OpenAI ──────────────────────────────────────────────────
OPENAI_API_KEY=sk-your-openai-api-key-here
LLM_MODEL=openai/gpt-4o

# ─── GitLab ──────────────────────────────────────────────────
# GitLab rootユーザーの初期パスワード（英数字+記号8文字以上）
GITLAB_ROOT_PASSWORD=Password1234!

# setup.sh 実行後に自動記入される
GITLAB_TOKEN=

# ─── Webhook ─────────────────────────────────────────────────
# GitLab Webhook のシークレットトークン（任意の文字列、空でも動作するが推奨）
WEBHOOK_SECRET=change-me-to-random-string

# ─── トリガー設定 ──────────────────────────────────────────────
# Issue にこのラベルが付くと OpenHands が起動する
TRIGGER_LABEL=openhands

# コメントにこの文字列が含まれると OpenHands が起動する
TRIGGER_MENTION=@openhands

# ─── Docker ホストアドレス（setup.sh が自動設定） ──────────────
# Mac  → host.docker.internal  （Docker Desktop が自動解決）
# Linux → Docker bridge gateway IP（例: 172.17.0.1）
# ※ setup.sh を実行すると自動的に検出・書き込みされるため手動設定不要
DOCKER_HOST_INTERNAL=
